@model IEnumerable<aspBattleArena.Models.Organization>

@{
    ViewData["Title"] = "Index";
}

<h1>Organizations</h1>

@if (User.Identity.IsAuthenticated)
{
    <p>
        <a asp-action="Create">Create New</a>
    </p>
}
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                Country of Origin
            </th>
            <th>
                Boss
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CountryOfOrigin)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NameOfBoss)
            </td>
            
            <td>
                <a asp-action="Edit" asp-route-id="@item.OgranizationId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.OgranizationId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.OgranizationId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
